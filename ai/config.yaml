app:
  name: "Homepot AI Companion"
  version: "1.0.0"
  host: "0.0.0.0"
  port: 8001

llm:
  model: "llama3.2"
  base_url: "http://localhost:11434"
  temperature: 0.7
  context_window: 4096

memory:
  chroma_path: "data/chroma_db"
  collection_name: "device_logs"
  embedding_model: "all-MiniLM-L6-v2"

database:
  url: "postgresql://user:password@localhost:5432/homepot"

anomaly_detection:
  sensitivity: 0.8
  window_size: 50
