# Python
__pycache__/
*.py[cod]
*$py.class
*.pyc
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
/lib/
/lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
*.manifest
*.spec
# The User App PyInstaller build specs (packaging/) are committed (CI + local packaging uses them).
!packaging/*.spec
# PyInstaller build output for the User App (binaries shipped via extraResources).
# Kept outside `user_app/dist` because vite empties `dist/` on each build.
pyinstaller-dist/

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Translations
*.mo
*.pot

# Django stuff
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff
instance/
.webassets-cache

# Scrapy stuff
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
.python-version

# pipenv
Pipfile.lock

# poetry
poetry.lock

# pdm
.pdm.toml
.pdm-python
.pdm-build/

# PEP 582
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.venv/
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Project specific
config/secrets/
*.key
*.pem
*.crt
certificates/
secrets.json
private.json

# Scripts directory - only bash scripts (.sh) should be committed
# Python scripts (.py) in scripts/ are for local use only
scripts/*.py
scripts/**/*.py

# Docker
docker-compose.override.yml

# Terraform
*.tfstate
*.tfstate.*
.terraform/

# Local development
.local/
temp/
tmp/
logs/*
!logs/README.md
kpi-evidence/*
!kpi-evidence/README.md

# PostgreSQL - Database managed by system service
# Database location: /var/lib/postgresql/16/main/
# Use pg_dump for backups (see docs/database-guide.md)

# GitHub Workflows - Downloaded artifacts and reports
dependency-health-report-*/
security-audit-report-*/
workflow-artifacts-*/
test-results-*/

# Backup files created by upgrade processes
backup-requirements.txt
pre-upgrade-freeze.txt
test-upgrade/
test-major-upgrade/

# Dependency analysis temporary files
outdated.txt
security-audit.json
pipdeptree-output.txt

# Node.js / Frontend (merged from PR #5 and health checks)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
# NOTE: We now commit frontend/package-lock.json for reproducible builds and CI caching.
# The user_app lockfile is committed too so `npm ci` works in the packaging CI job.
# Only ignore package-lock.json outside of frontend/ and user_app/
/**/package-lock.json
!frontend/package-lock.json
!user_app/package-lock.json
.npm
.yarn-integrity

# Frontend build outputs
dist/
dist-ssr/
*.local
build/
.next/
out/
.nuxt/
.cache/
coverage/
# The user_app app icons are source assets committed to the repo so CI packaging
# embeds the real brand icon instead of the default Electron icon.
!user_app/build/
!user_app/build/icon.icns
!user_app/build/icon.ico
!user_app/build/icon.png

# Frontend environment
.env.local
.env.*.local

# Android / Gradle build caches (user_app/android)
.gradle/
android/
**/android/.gradle/
**/android/local.properties

# VSCode
.vscode/settings.json
*.db
*.sqlite3
*.db-journal
ai/data/

# UQ — campaign working directories and runtime-generated plots (recreated on each run)
uq/study1_anomaly/campaign_anomaly_uq/
uq/study2_simulator/campaign_*/
uq/study3_push/campaign_*/
